CREATE TABLE rendimentos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    tipo VARCHAR(100),
    quantidade INT
);


CREATE TABLE despesas (
    id INT AUTO_INCREMENT PRIMARY KEY,
    tipo VARCHAR(100),
    quantidade INT
);